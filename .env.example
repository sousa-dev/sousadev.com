# Proposal form delivery. Set these in the host's environment, never in the repo.
# Without them the endpoint answers 503 and the form reports that nothing was sent.
RESEND_API_KEY=
PROPOSAL_TO=hello@sousadev.com
PROPOSAL_FROM=
